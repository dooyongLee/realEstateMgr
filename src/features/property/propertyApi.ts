// 중복 매물 체크 API (예시)
export async function checkDuplicateProperty({ address, type, dealType }) {
  // 실제 API 연동 필요
  // const res = await fetch(`/api/properties/check-duplicate?address=${encodeURIComponent(address)}&type=${type}&dealType=${dealType}`);
  // const data = await res.json();
  // return data.isDuplicate;
  // 임시: 항상 false 반환
  return false;
} 